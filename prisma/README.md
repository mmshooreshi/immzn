npx prisma migrate dev --name init  # Create & apply the migration
npx prisma generate                 # Optional, to refresh client
npx prisma studio                   # Optional, to inspect DB visually
pnpm dev                            # Restart your Nuxt app
